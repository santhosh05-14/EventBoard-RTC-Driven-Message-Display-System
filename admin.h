/* Function to edit RTC settings (hour, minute, second, date, month, year) */
void RTC_Edit();

/* Function to enable or disable events in event list */
void Event_Edit();

/* Admin mode main function (RTC edit / Event edit / exit control menu) */
void admin_Mode();
